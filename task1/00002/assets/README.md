# Terraform asset layout

Terraform input files are owned by their root module under this directory. For example, the foundation module reads `assets/foundation/...`; generated ZIP files remain outside `assets/`.
