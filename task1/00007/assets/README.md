# Terraform asset layout

Terraform input files are owned by their root module under this directory. Foundation and addons assets are kept in `assets/foundation/` and `assets/addons/`; generated ZIP files remain outside `assets/`.
