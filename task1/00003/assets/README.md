# Terraform asset layout

Terraform input files are owned by their root module under this directory. Platform and addons assets are kept in `assets/platform/` and `assets/addons/`; generated ZIP files remain outside `assets/`.
