# Terraform asset layout

Place future root-module assets under `assets/<root-module>/`. `assets/shared/` is reserved for inputs used by more than one module; it currently contains the official binary packaging Dockerfile.
