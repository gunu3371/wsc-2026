candidate_id = "00000"

# Optional: add the IAM role used by CloudShell/grading if it differs from the
# principal that creates the cluster.
additional_cluster_admin_principal_arns = [
  # "arn:aws:iam::123456789012:role/CloudShellRole"
]

# Pin only after confirming the EKS versions available in ap-northeast-2.
# kubernetes_version = "1.xx"
