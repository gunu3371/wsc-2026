{
  "DocumentName": "AWS-RunShellScript",
  "InstanceIds": ["i-0e39df9ee4bbcaf04"],
  "Parameters": {
    "commands": [
      "grep -B 3 -A 12 -E 'will be destroyed|must be replaced' /tmp/addons-reconcile.log || true"
    ]
  }
}
